pipeline {
  agent any
  stages {
    stage('Shell script 0') {
      steps {
        sh '''#!/bin/bash

cd Bash_scripts/HW28
SHELL_SCRIPTS=$(find . -name \'*.sh\')

for var in $SHELL_SCRIPTS ; do
shellcheck "$var"
done'''
      }
    }

  }
}